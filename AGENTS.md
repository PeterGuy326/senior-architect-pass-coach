# 仓库协作约定

本仓库实现“系统架构设计师过线私教”的员工包、确定性进度引擎和本地 Workbench。

## 不可破坏的边界

1. 教学行为以 `employee/senior-architect-pass-coach/SKILL.md` 和同目录下的 `schemas/` 为契约；过线线 45、安全目标 52，每次推荐最多 3 项。
2. 私人学习数据只能写入操作系统用户数据目录或用户明确指定的仓库外目录。不得把用户问题、答案、档案、状态或本地授权文件写进 Git 仓库。
3. 任何个人进度读写都需要有效的本地授权上下文；匿名上下文只能得到通用诊断，不能得到私人状态，也不能产生进度事件。
4. 员工包只读且只能提出进度事件。`service/` 必须先验证输出 Schema、动作、授权范围和本地可信证据，之后才可调用 `progress_engine` 写入。
5. 只把白名单字段组成的去标识化快照交给员工包。不得传递用户 ID、姓名、邮箱、自由文本档案、本地路径或资源路径。
6. 出题时，考生提交前不得展示答案或解析；提交后才能按员工包契约反馈。
7. Digital Employee 版本必须精确锁定。Codex 当前只能探测，不能作为可运行 Adapter；`run` 未交付前不得伪装成已上线能力。

## 修改后的最低验证

```bash
npm run check
```

不要用测试 fixture 之外的真实个人学习数据做测试。
